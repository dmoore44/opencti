apiVersion: extensions/v1beta1
kind: Ingress
metadata:
    name: "opencti"
    labels:
        app: "opencti"
    annotations:
        kubernetes.io/ingress.class: addon-http-application-routing
spec:
    rules:
        - host: openctif9ab-opencti.2c6d495bd7c54fcd9a57.westus.aksapp.io
          http:
            paths:
            - path: /
              backend:
                serviceName: "opencti"
                servicePort: 80